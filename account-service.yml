server:
  port: 8082

spring:
  application:
    name: ACCOUNT-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5433/Accountdb
    username: postgres
    password: Naveen@2471

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
